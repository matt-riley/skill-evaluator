# Implementation Plans

Plans 001–006 were generated by the improve skill on 2026-06-25 (all DONE).
Plans 007–016 were generated on 2026-07-01 from the agengit-integration
research (`docs/research/agengit-integration.md`). Plans 017–024 were
generated on 2026-07-01 from a gap/flaw review of the tool's own approach
(two confirmed bugs: 017, 018). Plans 025–030 were generated on 2026-07-01
from a documentation-site review (one confirmed docs-integrity failure:
the `feedback.json` guide documents an unimplemented feature — 026/027).
Execute in the order below unless dependencies say otherwise. Each
executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add Go test coverage to the CLI core | P1 | M | — | DONE |
| 002 | Validate `.skill-eval.yaml` against the existing JSON schema | P1 | S | 001 | DONE |
| 003 | Add deterministic assertion matchers to reduce judge cost and noise | P1 | M | 001 | DONE |
| 004 | Add structured `--verbose` logging for agent debugging | P2 | S-M | — | DONE |
| 005 | Add iteration lockfile and `--resume` for safe partial runs | P2 | M | 001 | DONE |
| 006 | Add cross-iteration benchmark diff to surface trends | P3 | S | — | DONE |
| 007 | Seed eval input files from agit blobs; hermetic run workdirs | P1 | L | — | TODO |
| 008 | Make `import-agit --merge` idempotent (dedupe by step hash) | P1 | S | — | TODO |
| 009 | Add `skill-eval validate` — lint SKILL.md against the Agent Skills spec | P1 | S-M | — | TODO |
| 010 | Use `agit eval --include-steps` for per-step quality filtering | P2 | S | — | TODO |
| 011 | Add `--runs N` repetitions for statistically honest benchmarks | P2 | M | — | TODO |
| 012 | Persist run transcripts; mine tool calls into process assertions | P2 | M | — | TODO |
| 013 | Activation evals — test skill discovery, not just execution | P2 | M | 009 | TODO |
| 014 | New import sources: `--between` git ranges and `--bundle` archives | P3 | S | 008 | TODO |
| 015 | Process-quality benchmarking via `agit eval` on the eval runs | P3 | M-L | — | TODO |
| 016 | LLM-assisted assertion authoring and skill-value report buckets | P3 | M | — | TODO |
| 017 | Fix `--baseline previous` — it compares the skill against itself | P1 | S-M | — | TODO |
| 018 | Benchmark integrity: count failed runs, guard iteration deltas | P1 | M | — | TODO |
| 019 | Harden the judge against output injection; canaries & consistency | P2 | M | — | TODO |
| 020 | Lint evals.json and add `run --dry-run` | P2 | S-M | 009 | TODO |
| 021 | Content-hash grading cache with `--force-grade` | P3 | M | — | TODO |
| 022 | `skill-eval gate` — CI regression thresholds + JSON verdicts | P2 | M | 018 | TODO |
| 023 | `skill-eval prune` — workspace retention without losing history | P3 | S | 017 | TODO |
| 024 | Skill-dir integrity guard and always-hermetic runs | P2 | S-M | 007 (Part B) | TODO |
| 025 | Evals & assertions masterclass (rewrite + assertion cookbook) | P1 | M | — | TODO |
| 026 | Docs truth reconciliation (fictional features, schemas, flag reference) | P1 | M | — | TODO |
| 027 | Implement the feedback lifecycle — note → judge/fix/report → assertion | P1 | M | — | TODO |
| 028 | Concepts & how-it-works pages (glossary + pipeline internals) | P2 | M | — | TODO |
| 029 | Troubleshooting & FAQ (symptom-indexed failure modes) | P2 | S-M | — | TODO |
| 030 | Example skill + zero-to-benchmark tutorial + learning path | P3 | M-L | 025, 026 | TODO |
| 031 | Fail fast on missing/invalid agent runtimes (preflight; kill `exec false` trap) | P2 | S | — | TODO |
| 032 | Honest token accounting — provenance labels, exclude unknowns from stats | P2 | S-M | 018 (soft) | TODO |

## Dependency notes

Cycle 1 (001–006):

- **002 before 001?** Plan 001 is foundational and can run first, but 002's validation tests are easier to write after 001. Either order works; the table lists 001 first to establish the test harness.
- **001 must precede 005** because the lockfile logic benefits from a working test command and new `workspace_test.go` coverage.
- **001 strongly recommended before 003** so the matcher logic has a place to live in `grader_test.go`.
- **004 is independent** but should avoid conflicting with flag-parsing changes in other plans; run it after 002 if 002 touches `main.go` flag handling.
- **006 is independent** but reads `iteration-N/benchmark.json`; if 005's lockfile is in place, 006 should skip iterations whose lock status is `"running"`.

Cycle 2 (007–016):

- **007 first for value, not for order.** Nothing depends on it structurally, but until it lands, benchmarks over imported evals grade replay noise — every other improvement measures against that noise floor.
- **008 must precede 014** — bundles and `--all-sessions` sync workflows re-import the same steps; without dedupe they duplicate the corpus.
- **009 must precede 013** — activation evals reuse 009's `parseSkillMD` frontmatter parser.
- **010, 012, and 014 all touch `internal/agit/convert.go` signatures** (`ConvertSteps` params in 010, `buildAssertionsWithSignals` params in 012, conversion reuse in 014). Land them in numeric order to avoid rebase churn; each plan notes the collision.
- **011 before 015 if both are scheduled** — 015 writes per-run artifacts and adopts 011's `run-<r>/` layout via `runConfigPath`; landing 015 first means reworking its paths later.
- **012 before 016 is soft** — 016's authoring prompt offers transcript matchers only if 012's prefixes exist (it feature-detects), so either order works but 012-first gives authored assertions more to work with.
- **007 + 014 interact**: bundle imports only carry fixture blobs if 007's restoration reads them through the shared conversion path; 014 adds a test for this once both are in.
- **Probe-first plans (007, 010, 014, 015)** each start with an agit interface probe and carry STOP conditions if the installed agit differs from the `docs/format/*.md` specs (written against agengit v1.26.0). Re-verify probes if agit has been upgraded since 2026-07-01.

Cycle 3 (017–024):

- **017 and 018 are bug fixes and jump the queue** — 017 (`--baseline previous` compares the skill against itself) and 018 (failed runs silently vanish from benchmarks) mean current numbers can be actively misleading; land them before investing in anything that consumes those numbers.
- **018 before 022** — the gate's `--max-fail-runs` check and the corpus-hash guard for `--against` read fields 018 introduces; a gate over biased numbers automates the bias.
- **017 before 023 (soft)** — prune's "snapshots are load-bearing" rule exists because of 017's always-snapshot convention; the rule is written defensively so either order is safe.
- **009 before 020 (hard)** — the evals linter extends `validate`'s `Finding` model and command; do not fork a second findings mechanism.
- **019 and 021 both rework `gradeFromOutput`** (prompt structure + signatures in 019, cache short-circuit + signatures in 021); land in numeric order, and 021's cache key must include 019's canary/consistency knobs if 019 landed first (021 documents this).
- **018 and 013 both extend `computeBenchmark`'s signature** — whichever lands second rebases; both plans flag it.
- **024 Part A is independent; Part B needs 007's `prepareWorkdir`.** Part A (integrity detection) can ship alone if 007 is delayed.
- **Cross-cycle sequencing suggestion**: 017 → 018 → 008 → 009 → 020 → 007 → 010/011/012 → 019 → 013 → 022 → 021 → 014 → 024 → 015 → 016 → 023. Front-loads correctness of the numbers, then import quality, then trust and CI, then ergonomics.

Cycle 4 (025–030, documentation):

- **026 and 027 resolve the same lie from two sides**: `giving-feedback.md` documents an unimplemented `feedback.json`. 027 implements the feature (preferred fix); 026 quarantines the guide with an honest banner if 027 hasn't landed yet. Whichever lands second reconciles the guide — both plans carry instructions for either ordering, but landing **027 before or with 026** avoids the banner entirely.
- **027 goes beyond what the docs promised** — full lifecycle: scaffold → judge context (both configs, identically — fairness invariant) → fix critique → report debt view → judge-assisted promotion of notes into assertions (`skill-eval feedback --promote --write`). Promotion overlaps Plan 016's authoring validator — whichever lands second reuses the first's helper.
- **025 owns `writing-evals.md`; 026 owns every other page.** The one factual error inside writing-evals (`--baseline previous` misused as "run without skill") is fixed in 025 to avoid double edits. Don't let both plans touch the same file.
- **030 depends on 025 and 026 (hard)** — the tutorial reuses 025's running example corpus and must teach 026's corrected semantics; it is the capstone, do it last.
- **028 and 029 are independent** but read better after 026 (they link into corrected pages and must document only shipped behavior — both carry check-the-plan-status instructions).
- **Docs plans track code plans**: several entries change wording depending on whether 007 (files semantics), 011 (stddev caveats), 017 (baseline semantics), or 020 (lint errors) have landed. Every affected step says "check `plans/README.md` status first" — keep this table current or the docs will drift the day they're written.
- **Tone contract for all of cycle 4**: keep the playful emoji voice, but every claim must sit within one screen of a concrete artifact (real JSON, real command output, provenance-commented). "Show, then tell."

Cycle 5 (031–032, from the codebase-quality review):

- **031 and 032 both touch the `AgentRunner` interface** (`BinaryName()` in 031, `ExtractTokens()` in 032) — land in numeric order, or settle the interface shape once if reviewed together.
- **032 after 018 (soft)** — both move the same `aggregateRuns`/`TimingData` lines; both fields are additive, but whoever lands second rebases.
- **029 ↔ 031 interlock**: 029 documents the missing-agent trap as it exists; 031 fixes it and changes the symptom. Whichever lands second updates the troubleshooting entry — both plans say so.

## Findings considered and deferred (cycle 5)

- **Split `package main` into internal packages** (grader/runner/benchmark/workspace): the root package is straining at ~6k lines and the `internal/agit` split was the right instinct — but ~20 pending plans pin file paths and line-anchored drift checks to the current layout. Restructuring now would invalidate most of the backlog. **Deliberately deferred until the plan backlog substantially drains**; when picked up, it should be a mechanical move-only change with no behavior edits, done in one PR.
- **Token → dollar cost conversion**: pricing tables rot; parked as a 032 follow-up behind a user-supplied price table.
- **`skill-eval doctor`**: parked as a 031 follow-up (preflight + config validation + agit presence in one diagnostic).

## Findings considered and rejected

- **YAML → JSON config**: user explicitly prefers YAML; not converted.
- **Multi-skill registry / discovery**: beyond v1 core loop; defer.
- **Output caching by content hash**: high value but adds significant complexity; defer until lockfile/resume exist.
- **New `trend` subcommand**: defer; this plan cycle only enriches `benchmark.json` (Plan 006).
